{
  "name": "caja-diaria",
  "private": true,
  "version": "1.0.0",
  "description": "Control de caja diaria — wrapper nativo (Capacitor) para App Store / Google Play. El sitio web en sí vive en la raíz del repo y no necesita build; ver MOBILE.md.",
  "scripts": {
    "prepare:mobile": "bash scripts/prepare-mobile-www.sh",
    "sync": "npm run prepare:mobile && cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/local-notifications": "^8.2.1",
    "@capgo/capacitor-native-biometric": "^8.6.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
