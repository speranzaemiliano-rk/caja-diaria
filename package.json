{
  "name": "caja-diaria",
  "private": true,
  "version": "1.0.0",
  "description": "Control de caja diaria — wrapper nativo (Capacitor) para App Store / Google Play. El sitio web en sí vive en www/ y no necesita build.",
  "scripts": {
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
