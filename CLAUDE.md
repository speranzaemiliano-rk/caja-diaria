# Caja Diaria — memoria del proyecto

App web para **control de caja diaria** de comercios (kioscos, locales, freelancers): registrar
ingresos/egresos por día, ver saldo en vivo, arqueo, préstamos entre empresas, exportar y respaldar.
Idioma: **español rioplatense** (voseo: "registrá", "cerrá", "probá").

## Cambios recientes (v82–v85, 2026-07) — leer esto: actualiza bullets viejos de más abajo

- **v82 · El crédito suma al saldo final (cambia el modelo de `sf`).** Antes `sf = si + ing + egr` (solo efectivo). Ahora **`sf = si + ing + egr + credAdj`**: al fiar (salida de efectivo + un crédito a cobrar), el efectivo baja como egreso pero el crédito lo compensa, así el **Saldo final ya no baja** — refleja el total (efectivo + créditos). `cascadeChain()` es la fuente de verdad y encadena `sf` día a día incluyendo el crédito acumulado. En consecuencia:
  - La **diferencia de caja** quedó como `dif = sf − (fis + cred)`, que ahora equivale a **efectivo esperado − efectivo contado** (el crédito se cancela de los dos lados) — mide solo el descuadre del efectivo. En pantalla, "Según sistema" (`sf`), "Caja real" (`fis+cred`) y la diferencia cierran entre sí.
  - Para obtener el efectivo puro de un día: `efectivo = sf − cred`. Los desgloses "Efectivo · Crédito" de los KPIs (`Efectivo = sf − cred`) ahora dan bien.
  - Días **sin** créditos quedan idénticos. Los días viejos se recalculan solos al abrir (via `recompute()`→`cascadeChain()`), no hay que re-guardar nada.
  - ⚠️ Esto **actualiza** los bullets de más abajo que dicen "los números grandes no cambian / `sf` es el cálculo de movimientos de siempre / `dif = sf − fis`" — quedaron viejos.
- **v82 · Borrar un día no "revive" al sincronizar (lápida).** `deleteDay` ya no saca el día del array (la fusión con la nube lo re-agregaba): deja un **día vacío** (`empty:true`) con sello `mod` nuevo, así para la fusión es la versión más reciente y el borrado se propaga. `recompute()` filtra los vacíos.
- **v83 · El detalle de créditos ("Ver") acotado al día.** `creditNotesBreakdown(ctxId,cur,hastaIso)` y `creditNoteEntries(note,hastaIso)` toman una fecha opcional: abierto desde un día puntual, solo suma los créditos **hasta esa fecha** (así entrar al 7/7 no muestra un crédito dado el 24/7). `openCreditDetail(hastaIso)` guarda el alcance en `creditDetailScope` para que los re-render internos (editar/eliminar un ajuste) no vuelvan a la vista global. Sin `hastaIso` sigue siendo la vista global histórica. Esto **actualiza** el bullet que dice que el desglose suma "todos los días reales".
- **v84 · Hoja de cálculo flotante** (calculadora tipo cinta). Bloque autocontenido al final de `app.html` (buscar `mcalc`/`evalLine`): botón flotante abajo a la derecha → panel arrastrable, una cuenta por línea (`+ - * / ( ) %`), total en vivo, Copiar/Limpiar. 100% local (guarda contenido/posición en `localStorage`, no toca la caja ni la nube). La evaluación **sanitiza** a solo dígitos y operadores antes de `Function()` (no ejecuta código). Queda debajo del candado (`pinGate`).
- **v85 · Columna "Diferencia" en el resumen por días** (`renderIndex`, tabla del Índice): muestra `d.dif` con color (rojo si `isWarn`, verde si chico, "—" si no hay arqueo) después de "Saldo final".
- **Multiusuario pendiente (P1, ver `SECURITY.md` y `AUDITORIA-2026-07.md` en el repo del Sistema):** borrar un **préstamo** o una **empresa** todavía puede revivir al sincronizar (falta darle la misma lápida con `mod` que ya tienen los días; `fusionarCajas` los une por id sin lápida).

## Qué hay en el repo (sitio estático, sin build)

**El sitio web vive en la raíz del repo, siempre** (`index.html`, `app.html`,
`manifest.json`, `sw.js`, íconos, `biletes.avif`) — **se intentó mover a una carpeta
`www/`** para que conviva más prolijo con el wrapper nativo de Capacitor, pero **rompió el
sitio en vivo** (dos veces: GitHub Pages acá resulta estar sirviendo con "Deploy from a
branch" directo del árbol de `main`, no con el workflow de Actions como se había asumido
— al sacar los archivos de la raíz dejaron de estar donde ese mecanismo los busca) y se
revirtió. **No volver a mover estos archivos de la raíz.** Ver el bullet de Capacitor más
abajo y `MOBILE.md` para cómo conviven con el wrapper nativo sin necesidad de moverlos.

| Archivo | Qué es |
|---|---|
| `index.html` | **La portada** (landing, página aparte): estilo **editorial oscuro** (tipografía serif **Fraunces** en títulos + Inter/Roboto Mono) inspirado en una infografía tipo revista. **Rebranding "Mess" (pedido explícito del usuario, le pasó un kit de marca — `MARCA.md` — de otro producto propio para reusar su identidad visual acá, mientras la app sigue siendo "Caja Diaria", no se renombra)**: fondo casi negro violáceo `#0A0D14`, acento **violeta** `#9E86FF`/`#7C5CFF` (antes azul `#6aa5ff`), rampa `--s1..--s5` violeta oscuro→lila claro (antes acero gris). El hero tiene un **medio-anillo** (SVG, generado por JS en el `<script>` final: `#donut`, misma rampa violeta) mostrando la "Composición de ingresos · ejemplo de un mes" con líneas guía a cada etiqueta (%, monto) y el total al centro, más un sello serif "Caja · Diaria". Botón "Abrir mi caja" → `app.html`. Registra el SW. NO tiene la app embebida. Ver bullet de **`logo.svg`** más abajo para el logo nuevo (header, footer y una marca de agua grande y animada detrás del hero, `.hero-mark`). |
| `logo.svg` | **Logo de marca** (átomo/órbitas violeta-cian-lila, animado con SMIL `<animateTransform>` — las órbitas giran, los nodos giran al revés más lento, la esfera late). Copiado **tal cual** del SVG que el usuario pasó en `MARCA.md` (kit de marca de "Mess", otro producto propio del usuario) — solo se le cambiaron los `id` de los degradados (`messSphere`→`cdSphere`, etc., prefijo `cd` de "Caja Diaria") para no chocar si en algún momento se inlinea junto a otro SVG con esos mismos `id`, y el `aria-label` a "Caja Diaria". Se usa como `<img src="logo.svg">` (nunca inline) en 3 lugares de `index.html`: header (`.brand .mk`, 34px), footer (28px) y una marca de agua grande semi-transparente detrás del hero (`.hero-mark`, ~260px, `position:absolute`, `pointer-events:none`, por eso no interfiere con los clicks del hero). Al ser `<img>` (no inline) la animación SMIL sigue andando igual — motivo explicado en el propio `MARCA.md`: los navegadores sí ejecutan SMIL dentro de un `<img src>`, a diferencia de CSS. Cacheado en `sw.js` → `ASSETS`. |
| — | **Splash de entrada** (`#splash`, primer hijo de `<body>` en `index.html`, pedido explícito del usuario: "un inicio solo de Mess con el logo de 4 segundos y luego abrir la caja"): overlay fijo a pantalla completa (`position:fixed;inset:0;z-index:999`) con el logo animado + el texto **"Mess"** (en degradado cian→violeta→lila, el mismo gradiente de marca del kit) + "Sistema de Gestión" — el nombre y el texto del splash son literalmente los del kit de marca (`MARCA.md`), a pedido explícito del usuario, aunque la app siga siendo "Caja Diaria" en el resto del sitio (header, footer, `<title>`, todo igual). **No navega a ningún lado**: `index.html` ya está cargado completo detrás del splash, así que "abrir la caja" acá significa simplemente revelar la portada de siempre (con su botón "Abrir mi caja" para entrar a `app.html`) — un `setTimeout` de 4000ms le agrega la clase `.hide` (fade con `transition:opacity .6s`) y saca `overflow:hidden` del `<body>` (bloqueado mientras se ve el splash para que no se pueda scrollear detrás), y 650ms después (una vez terminada la transición) lo saca del DOM del todo con `.remove()`. Se muestra **siempre** que se carga `index.html` (no hay lógica de "solo la primera vez" con `sessionStorage` ni nada — no se pidió, y sumarlo sería una decisión de producto que no me correspondía tomar sola). Solo vive en `index.html` — `app.html` no tiene splash, arranca directo. |
| `app.html` | **La caja** (la app): "Control de Caja Diario · Movimientos". Tema claro sobrio (fondo `#f6f7f9`, acento navy `#2c4a75`/`#213a5c` en vez del azul "Google" genérico de antes, verde `#1f7a44`/rojo `#c53434`, Roboto + Roboto Mono) — rediseño pedido explícitamente por el usuario ("más profesional y más limpio"): paleta menos saturada, más aire en paneles/tablas/KPIs (`padding`/`gap` subidos ~10-15%), y los emojis multicolor del sistema (🖨✉☁💾💱🧾⚠️✈📲🗑, que el navegador pinta con su propio glifo a color ignorando el CSS) reemplazados por un set chico de íconos SVG lineales (`ICON_SVG`/`ic(name,size)`, trazo en `currentColor`) — ver bullet de íconos más abajo. Los símbolos Unicode monocromos (▲▼◆✎✕☑⌕etc.) NO se tocaron: ya heredaban el color del texto y no rompían la paleta. El reskin base se hizo cambiando `:root` + un bloque de overrides al final del `<style>` (conservando toda la estructura/JS). El logo del sidebar (`a.brand`) vuelve a la portada. El ícono (`.brand .mark`) es una **foto real** de fajos de billetes de US$100 (`biletes.avif`, subida por el usuario directo al repo vía GitHub web — no hay forma de recibir archivos de imagen desde la conversación de Claude Code) mostrada como `background-image` recortado/con zoom (`background-position:38% 12%;background-size:300%`, elegido probando varios recortes a tamaño real de 42px) — primero se probó un SVG vectorial pero el usuario prefirió la foto real. **Ojo**: hay un bloque de overrides más abajo en el `<style>` que originalmente pisaba este fondo con azul sólido (`.brand .mark{background:#1a73e8}`) — hay que mantenerlo sin `background` para no tapar la foto. `biletes.avif` está en `sw.js` → `ASSETS` para que cachee offline. |
| `manifest.json` | PWA. `start_url: "./"` → la app instalada abre la **portada**. `scope: "./"`. Splash blanco (`#ffffff`). **Ojo**: si en algún momento se corre `npx capacitor-assets generate` (parte del wrapper nativo, ver más abajo), ese comando puede reescribir este archivo agregando referencias a íconos PWA propios en rutas que no existen — ya pasó una vez, se detectó comparando contra el original y se restauró a mano. Revisar el diff de este archivo después de correr ese comando. |
| `sw.js` | Service worker. Para HTML (navegaciones): **red primero** con `fetch(request,{cache:'no-store'})` — el `no-store` es necesario porque GitHub Pages manda `cache-control: max-age=600` en `app.html`/`index.html`; sin él, la caché HTTP del navegador podía devolver una respuesta vieja sin llegar siquiera a la red, y un usuario que recargaba después de un deploy seguía viendo la versión anterior hasta 10 minutos (bug real, diagnosticado cuando un fix de UI no se veía recién publicado). Para el resto de los assets (manifest, íconos, `biletes.avif`): cache-first con revalidación en segundo plano. **Al tocar HTML, subí `CACHE` (`caja-diaria-vN`)** para purgar la caché vieja del Cache Storage (`activate` borra cualquier caché con nombre distinto al actual). **Gotcha relacionado, mismo síntoma ("no veo el cambio")**: GitHub Pages manda ese mismo `cache-control: max-age=600` para el propio `sw.js` (no solo para el HTML) — el `fetch(...,{cache:'no-store'})` de arriba resuelve el HTML siempre fresco, pero no evita que el navegador siga corriendo hasta 10 minutos una versión **vieja del propio service worker** (con su lógica de fetch vieja) si no revalida el archivo `sw.js` en sí. Fix: `navigator.serviceWorker.register('sw.js',{updateViaCache:'none'})` en `app.html` e `index.html`, que fuerza al navegador a no usar nunca la caché HTTP para chequear si hay una versión nueva de `sw.js`. |
| `icon-192.png` / `icon-512.png` | Íconos de la PWA. También son la fuente (copiados a `assets/icon.png`) de los íconos/splash nativos de Android e iOS — ver bullet de Capacitor. |
| `biletes.avif` | Foto real de fajos de billetes de US$100, usada como logo del sidebar en `app.html` (ver bullet de `app.html`). Subida por el usuario directo al repo. |
| `.github/workflows/deploy-pages.yml` | Deploy automático a GitHub Pages en cada push a `main` (usa `configure-pages` con `enablement:true`, sube `path: '.'` — la raíz del repo). |
| `capacitor.config.json`, `package.json`, `android/`, `ios/`, `assets/`, `scripts/prepare-mobile-www.sh`, `MOBILE.md` | **Wrapper nativo (Capacitor)** para subir la misma app a Google Play y al App Store, agregado a pedido explícito del usuario ("mejorarlo para poder subirlo a appstores"). **No mueve ni toca los archivos del sitio en la raíz** — `scripts/prepare-mobile-www.sh` arma una **copia** aislada en `mobile-www/` (generada, gitignoreada, nunca se commitea) que Capacitor usa como `webDir`, para no meter `.git/`/`node_modules/`/`android/`/`ios/` adentro del APK/IPA si se apuntara directo a la raíz. Detalle completo (cómo compilar, firmar y publicar en cada tienda, qué se verificó y qué no) en **`MOBILE.md`**. |

- **Sin frameworks, sin build, sin dependencias externas** (excepto EmailJS opcional, cargado on-demand) **para el sitio en sí** — el wrapper nativo de Capacitor (`android/`, `ios/`) sí tiene sus propias dependencias (Gradle, Swift Package Manager) pero vive aparte, en una copia generada (`mobile-www/`), y no afecta cómo se edita/despliega el sitio (HTML+CSS+JS inline en un solo archivo por página, sin build, en la raíz, tal como siempre).
- **Persistencia = `localStorage`** (los datos viven en el navegador del usuario; no hay backend).

## Publicación

- **Sitio en vivo:** https://speranzaemiliano-rk.github.io/caja-diaria/ (home) · `/app.html` (app).
- GitHub Pages está activado vía GitHub Actions. **Cada merge a `main` redepliega solo** — no hay que tocar Settings.
- La app es **PWA instalable** desde el home (Chrome/Edge → Instalar).
- **Apps nativas (Google Play / App Store)**: ver `MOBILE.md` para la guía completa. Resumen: el build de **Android se probó en este entorno y compila un APK real y funcional** (verificado con `aapt dump badging`); el de **iOS no se pudo compilar acá** (hace falta Xcode en una Mac) pero la estructura del proyecto quedó verificada como válida. Publicar en cualquiera de las dos tiendas requiere cuentas de desarrollador pagas (Google Play Console, USD 25 único; Apple Developer Program, USD 99/año) y, para iOS, una Mac — nada de eso se puede hacer desde acá. **Importante**: cualquier cambio en el sitio necesita `npm run prepare:mobile && npx cap sync` para llegar a los proyectos nativos antes de recompilar una versión nueva para las tiendas — no pasa solo.


## Modelo de datos (claves de localStorage)

- **Contextos** (empresa/proyecto): `caja_contexts_v1` → `[{id, empresa, proyecto}]`. Default: `{id:'default', empresa:'Mi Empresa', proyecto:'Principal'}`.
- Contexto/moneda activos: `caja_active_context_v1`, `caja_active_currency_v1` (`ARS`|`USD`).
- **Días por caja:** `caja_user_days_v2::<ctxId>::<CUR>` → array de días `{iso,date,wd,si,sf,ing,egr,dif,fis,caja:[números],chq,credSi,cred,credAdj,credMovs:[{imp,note}],movs:[{imp,cpt,ref}], src:'user', ...}` (imp<0 = egreso). `fis` = suma de `caja` (partes del conteo físico, cargadas por separado en el editor).
- **Histórico importado (base):** `BASE_SEED` embebido en el código (solo `default`+`ARS`). No se respalda (es estático). `baseForCtx(ctxId,cur)` lo devuelve.
- **Préstamos entre empresas:** `caja_loans_v1` → `[{id,iso,from:{id,empresa,proyecto},to:{...},cur,amount,note}]`. `registerLoan()` genera egreso en origen + ingreso en destino (ref `PRESTAMO#<id>`). Ese `ref` es un id técnico — se guarda tal cual (nunca se toca: `removeLoanMovements`/`deleteLoan`/`editLoan` lo necesitan para encontrar y revertir los 2 movimientos) pero **nunca se muestra al usuario así**: `refLabel(ref)` lo traduce a "Préstamo" en todos los lugares de solo-lectura (`renderDayMovs`, `openDay`, `renderMovs`/Buscar movimientos, el `confirm()` de borrar movimiento, y las exportaciones CSV/Excel). El único lugar que sigue mostrando el valor crudo es el input de "Referencia" del editor de movimientos (`#editor input.ref`) — ahí es un campo editable de verdad, no una vista, y traducirlo ahí rompería el guardado.
- **Backup por email:** `caja_backup_email_v1`, `caja_backup_enabled_v1`, `caja_backup_last_v1`, `caja_backup_method_v1` (`google`|`emailjs`|`draft`), `caja_backup_url_v1`, `caja_ejs_public/service/template`.

## Funciones clave (todas en `app.html`)

- **Set de íconos SVG** (`ICON_SVG`/`ic(name,size)`, ~línea 632): reemplaza los emojis multicolor que el navegador renderiza con su propio glifo a color (🗑☁✉🖨💾💱🧾⚠️✈📲), que rompían la paleta controlada. `ic(name,size)` devuelve un `<svg>` inline (`viewBox="0 0 24 24"`, `stroke="currentColor"`) para usar dentro de template literals JS (`${ic('trash',14)}`); los lugares donde el emoji/ícono estaba en HTML **estático** (no generado por JS) tienen el `<svg>` pegado literal en vez de una llamada a `ic()`, porque ese HTML se parsea antes de que el `<script>` corra. Iconos disponibles: `trash`, `cloud`, `mail`, `printer`, `save`, `exchange`, `receipt`, `alert`, `send`, `phone`, `grid`, `list`, `chart`, `search`. Los símbolos Unicode monocromos (▲▼◆✎✕☑→ etc.) se dejaron como estaban — heredan `currentColor` y no necesitaban reemplazo. Si se agrega un botón/ícono nuevo que el navegador podría pintar a color (revisar en varios SO), usar este mismo patrón en vez de tipear el emoji directo.
- **Navegación en grilla de íconos** (`.catnav`, arriba de todo el contenido, antes de `#warnBanner`): pedido explícito del usuario tomando como referencia el estilo de apple.com/store (imagen de referencia) — ícono grande en una placa redondeada + etiqueta abajo, en fila, en vez de la lista vertical que antes vivía en el sidebar. Los 5 botones (Resumen/Movimientos diarios/Análisis/Buscar movimientos/Préstamos) usan `class="nav-btn catnav-btn"` — **reusan la clase `.nav-btn`** a propósito para no tocar el JS de navegación ya existente (`goToView()`, el `querySelectorAll('.nav-btn')` que wirea los clicks y togglea `.on`); `.catnav-btn` solo aporta el layout visual (columna, ícono arriba). El sidebar quedó liviano: solo logo, selector de empresa/proyecto/moneda y el pie — la sección "Panel" con la lista de botones se eliminó del HTML. En mobile la grilla se envuelve sola (`flex-wrap`) en 2-3 filas y queda siempre visible arriba del contenido (no hace falta abrir el menú hamburguesa para cambiar de vista, a diferencia de antes donde la navegación vivía en el sidebar colapsable).
- **Tipografía y espaciado "grandes y con aire"** (pedido explícito del usuario, misma referencia de apple.com/store): el título de cada vista (`.topbar h2`) pasó de 21px/600 a 32px/800 con letter-spacing negativo; los números de las tarjetas KPI (`.kpi .num`) de 25px/700 a 27px/800; `--r` (radio de borde) de 10px a 14px; más padding en `.wrap`/`.kpi`/`.panel`. **Ojo con el ancho mínimo de columna de `.kpis`** (`grid-template-columns:repeat(auto-fit,minmax(230px,1fr))`): con números grandes en `Roboto Mono` bold, una columna angosta hace que el `$` se corte a otra línea (bug real, visto al agrandar la tipografía) — `.kpi .num` tiene `white-space:nowrap` a propósito para evitarlo; si se vuelve a tocar el tamaño de fuente de `.kpi .num`, hay que revisar que siga entrando en una sola línea a 4-5 columnas.
- **Editor de día** (`openEditor`/`saveDay`): "＋ Nuevo día". Incluye una sección **"⇄ Préstamo entre empresas"**:
  al guardar, `registerLoan()` agrega el egreso a ese día y el ingreso a la caja destino. El resumen refleja los préstamos pendientes en vivo. **Sin doble contabilidad.**
  A diferencia del modal de detalle (`#overlay`, cierra con click afuera), el overlay del editor (`#edOverlay`) **no tiene** handler de click-afuera-cierra — cargar un día/movimiento lleva rato y un click accidental fuera del panel no debe tirar todo lo tipeado (pedido explícito del usuario). Para salir están la ✕, "Cancelar" (ambos con `onclick="closeEditor()"`) o Escape (`keydown` global, sigue cerrando ambos overlays).
  El campo **Fecha** (`#f_date`) se puede editar tanto al crear un día nuevo como al editar uno existente (antes quedaba `disabled` al editar, sin motivo documentado — la lógica de guardado ya soportaba el cambio). `saveDay()` ya maneja bien el caso: detecta choque con otro día real en la fecha destino (`clash`, bloquea con error y no cierra el editor), y si no hay choque reemplaza la entrada de `userDays` en el mismo índice pero con el `iso` nuevo — `cascadeChain()` reordena y recalcula la cadena de `si`/`sf` para todos los días reales según la nueva fecha, así que mover un día no deja duplicados ni desalinea los saldos de los demás días.
- **"💱 Cambio de divisa"** (`registerExchange`, sección en el editor de día debajo de "Préstamo entre empresas"): compra/venta de dólares **dentro de la misma empresa/proyecto** — mismo patrón que un préstamo, pero entre las 2 monedas de un mismo contexto en vez de entre contextos, reutilizando `applyLoanMovement`. Campos: Tipo (Venta de divisa = doy US$ y recibo $ / Compra de divisa = doy $ y recibo US$), Importe en US$, Cotización ($/US$) — el importe en $ se calcula solo (`recalcFxPreview`). Se acumulan en `edState.fx` (lista, botón "＋ Sumar", igual que `edState.loans`) y se aplican al guardar el día (`registerExchange` por cada uno): genera un movimiento en la caja $ y otro en la caja US$ de ese mismo contexto, con concepto **"Venta de divisa"** / **"Compra de divisa"** (+ nota opcional) y `ref` fijo **"Cambio"** (sin id único: es solo una etiqueta, no se usa para buscar/revertir movimientos como sí pasa con `PRESTAMO#<id>`). `recalcEditor()` sí refleja en vivo el ingreso/egreso pendiente de estos cambios en el "Saldo final" previsualizado, según si la moneda que se está editando (`CUR`) es la que entra o la que sale en esa operación.
- **Saldo de préstamos por empresa/proyecto** (`ctxLoanNet(ctxId,cur)`): saldo neto de un contexto sumando **todas** las contrapartes (no por par) — positivo = le deben (prestó más de lo que recibió), negativo = debe. Se muestra en 2 lugares: (1) **badge en el sidebar** (`#ctxLoanBadge`/`renderCtxLoanBadge`, debajo del selector de Empresa/Proyecto) con el saldo del contexto **activo**, en cada moneda que tenga saldo pendiente — se actualiza al cambiar de empresa/proyecto (`renderCtxSelectors`) y al registrar/editar/eliminar un préstamo (`renderLoans`); (2) panel **"Resumen por empresa / proyecto"** en la vista Préstamos (`renderLoanSummary`, arriba de "Saldos entre empresas / proyectos"), que lista todas las empresas/proyectos con saldo pendiente. Convención de signo (ojo, es sutil): en `registerLoan`, quien **presta** (`from`) queda con un **egreso** y por eso **le deben**; quien **recibe** (`to`) queda con un **ingreso** y por eso **debe** — `ctxLoanNet` suma `+amount` cuando el contexto es `from` y `-amount` cuando es `to`. **Tanto el badge del sidebar como cada fila del "Resumen por empresa/proyecto" son clickeables** y abren `openLoanDetail(ctxId)`: un modal (usa `#modal`/`#overlay`, mismo patrón que `openDay`) con el desglose **por contraparte** — "▲ Le deben" (contrapartes que le deben, en verde) y "▼ Le debe a" (contrapartes a las que les debe, en rojo) + saldo neto, todo separado por moneda. El neto por contraparte lo calcula `ctxCounterpartyNets(ctxId,cur)` (net>0 = esa contraparte le debe; net<0 = el contexto le debe a esa contraparte), resolviendo el nombre actual con `ctxLabelById` (cae al label guardado en el préstamo si el contexto ya no existe).
- **Vista "Préstamos" — editar/eliminar** (`renderLoanTable`): cada fila del "Historial de préstamos" tiene iconos **✎/🗑** (`.rowact`, igual patrón que días/movimientos). `deleteLoan(id)` y `editLoan(id)` usan `removeLoanMovements(loan)` para encontrar y quitar los 2 movimientos ligados (por `ref==='PRESTAMO#'+id`, en la caja origen y destino, buscando en `caja_user_days_v2::<ctxId>::<cur>` — **no** solo en la caja activa) y recalcular ese día (`ing`/`egr`/`nmov`/`sf`/`cascadeChain`) en ambos contextos — así no quedan movimientos huérfanos. `editLoan(id)` precarga el formulario de arriba (origen/destino/moneda/importe/nota/fecha) con `editingLoanId` seteado, cambia el botón a **"✓ Guardar cambios"** y agrega un **"Cancelar edición"**; al guardar, el handler de `loanSubmit` revierte el préstamo viejo y llama `registerLoan()` de nuevo con los valores nuevos (queda con un id nuevo, no importa: el historial no depende del id para mostrarse). `deleteLoan(id)` ahora sí revierte los movimientos (antes solo borraba el registro y los dejaba huérfanos).
- **Acciones por fila** (`.rowact` ✎/🗑): en "Movimientos diarios" (días) y "Buscar movimientos" (asientos), cada fila `src:'user'` muestra editar (`editDay`) y eliminar (`deleteDay` para días, `deleteMov(iso,idx)` para un movimiento — recalcula ing/egr/sf/nmov/dif y `cascadeChain`). Los días/movimientos del histórico importado (`src:'base'`) **no** muestran iconos (read-only). Los botones usan `event.stopPropagation()` para no disparar el modal de detalle de la fila.
- **🖨 Imprimir resumen del día** (`printDay(iso)`): botón "🖨 Imprimir" en dos lugares — el panel "Movimientos del día" del Resumen (`#dayPrintBtn`, siempre visible, imprime el día activo `last`, a diferencia de Editar/Agregar que solo aparecen si `src==='user'`) y dentro del propio modal de detalle (`openDay`, junto a la ✕). `printDay()` abre/actualiza el modal con `openDay(d)` y llama `window.print()`. El CSS `@media print` oculta todo (`.app`, `.tip`, overlays cerrados) y deja solo el `.overlay.on .modal` sin sombra/fondo/bordes redondeados a ancho completo, ocultando además la ✕ y los botones `.btn-ghost` del header (Editar/Imprimir) para que no salgan en el papel. El modal ya traía saldo inicial/ingresos/egresos/saldo final + tabla de movimientos + control de caja (sistema vs. real) — se le agregó **empresa/proyecto y moneda** en el subtítulo (`ctxInfo`, antes solo tenía fecha) para que la hoja impresa sea autocontenida y no dependa de la pantalla. Funciona con cualquier día, incluido el histórico (`src:'base'`, solo lectura).
- **Resumen (v-dash) = el día**: KPIs de Saldo inicial/Ingresos del día/Egresos del día/Saldo final (`renderKpis`). Los **números grandes NO cambian** (siguen siendo `d.si`/`d.ing`/`d.egr`/`d.sf`, el cálculo de movimientos de siempre) y tienen una línea chica debajo con el desglose **"Efectivo $X · Crédito $Y"** donde **Efectivo = número grande − Crédito** (no es un dato independiente: es el número grande neto de créditos) y **Crédito** = el acumulado de créditos correspondiente (`d.credSi`/`d.cred` en Saldo inicial/final; en Ingresos/Egresos del día, la suma de los `d.credMovs` de ESE día con signo +/−). Por construcción, **Efectivo + Crédito siempre sale igual al número grande** de esa tarjeta — es la forma en que el usuario pidió verlo ("saldo final es suma de efectivo + créditos"). Una 5ª tarjeta, más chica (`.kpi.small`, pedido explícito del usuario), muestra **"⚖ Diferencia de caja"** del día activo: el `d.dif` con color verde/rojo según `isWarn(d)` (o gris + "Sin arqueo cargado" si `d.fis==null`), y debajo "Según sistema $X" / "Caja real $Y" — a diferencia de la alarma (`#warnBanner`), esta tarjeta se ve **siempre** para el día activo, tenga o no una diferencia grande, sin tener que abrir el detalle del día. + panel "Movimientos del día" (`renderDayMovs`) con la tabla de movimientos y botones **"＋ Agregar movimiento"** (alta rápida sin abrir el editor completo: `openQuickAdd`/`saveQuickMov`, panel Tipo/Concepto/Importe inline) y **"✎ Editar día"** (`editDay`) — ambos solo visibles si `last.src==='user'`.
- **Alarma de diferencia de caja** (`#warnBanner`/`renderWarnBanner`, primer hijo de `.wrap`, fuera de las `.view` — por eso queda **visible en cualquier vista**, siempre arriba de todo, sin tener que ir a buscarla a Análisis): escanea **todas** las empresas/proyectos y monedas (no solo la caja activa) con `computeGlobalWarnings()`, que recorre `contexts` × `['ARS','USD']` llamando a `ctxRealDays(ctxId,cur)` (carga esa caja con `loadUserDaysFor`+`cascadeChain`+`baseForCtx`, siempre releyendo de `localStorage` — no pisa el estado de la caja activa) y filtra con el mismo umbral que `isWarn()`. **Solo mira el ÚLTIMO día cargado de cada caja**: un descuadre viejo con días conciliados después NO dispara la alarma (pedido explícito del usuario: "solo que aparezca la del día" — la primera versión mostraba el descuadre histórico más reciente con "+N más" y le resultaba ruido). Efecto colateral asumido: registrar un préstamo/movimiento en una fecha posterior al día descuadrado también la apaga (el día del préstamo pasa a ser el último, sin `fis` → `dif=null`). Cada ítem muestra **"Según sistema" vs "Caja real" vs "Diferencia"** (mismos rótulos/valores que el modal `openDay`: `sf` = saldo según movimientos, `fis+cred` = caja física + créditos = lo contado en la realidad) — no solo el número de la diferencia, para poder comparar de un vistazo qué dice el sistema contra qué hay en la realidad (pedido del usuario). Cada ítem es clickeable (`jumpToCaja(ctxId,cur,iso)`): cambia de empresa/proyecto y moneda activa si hace falta, navega al Resumen (`goToView('dash')`, reemplaza al handler viejo de `.nav-btn`) y abre el detalle de ese día (`openDay`). Se recalcula en cada `renderAll()`, así que cualquier alta/edición de día o préstamo la mantiene al día — por eso registrar un préstamo nuevo ahora también llama `renderAll()` (antes solo `renderLoans()`, que no tocaba Análisis ni la alarma).
- **Análisis rediseñada**: arriba de todo, **"Resumen por empresa / proyecto"** (`renderCtxSummaryCards`, grilla `#ctxSummaryCards` con clase `.kpi.ctxcard`) — una tarjeta clickeable por cada contexto con su saldo $ y US$ (último día real de esa caja, vía `ctxRealDays`) + la posición neta de préstamos (`loanNetLine`, si tiene). Click en la tarjeta = `jumpToCaja(ctxId,'ARS',null)` (cambia de caja y va al Resumen). Debajo, **"Préstamos entre empresas — detalle"** (`renderAnaLoanDetail`, `#anaLoanDetail`): KPIs generales (préstamos registrados, movido en $ y US$ histórico bruto, empresas con saldo pendiente) + una tabla con Le deben/Debe/Neto por moneda y cantidad de préstamos de cada empresa/proyecto (más detallado que el badge/resumen de la vista Préstamos, que solo muestra el neto) — cada fila abre `openLoanDetail(ctxId)`. Debajo, **"Crédito vs. efectivo"** (`renderCreditCashRatio`, `#creditCashRatio`): por cada caja con al menos un arqueo cargado, compara el `fis` (caja real) contra el `cred` (créditos a cobrar) del último día con `fis!=null`, como una barra apilada azul/roja con el % de crédito. El segmento de crédito de la barra usa `var(--red)` (gradiente `#8a2323→var(--red)`) — antes era un ámbar fijo (`#f0a020`/gradiente `#b45309→#f0a020`, elegido porque en el tema claro `--gold` está redefinido igual a `--blue` y usarlo hacía que las dos leyendas se confundieran), pero el usuario pidió explícitamente ("aquí me gustaría que en las barras los que deben figuren en rojo") que la barra en sí — no solo el texto — marcara en rojo las cajas con crédito pendiente, para que sea consistente con el resto de la UI donde rojo = deuda/pendiente. El texto **"N% crédito"** de cada fila (`.val.mono`) también se pinta en `var(--red)` cuando `pctCred>0` (pedido anterior, "que en rojo figuren los que deben", primero solo aplicado al texto); en `0%` queda con el color de texto normal. La leyenda de abajo (el cuadradito ■ de "Crédito a cobrar") también pasó a `var(--red)` para que coincida con el color real de la barra, y suma una nota "Rojo = cajas con crédito pendiente de cobro". Debajo, **"Deuda por préstamos entre empresas"** (`renderLoanDebtRatio`, `#loanDebtRatio`): mismo patrón visual (barra apilada azul/roja + %) pero para la **deuda** de préstamos entre empresas en vez del crédito a cobrar de clientes — el usuario pidió explícitamente "% de deuda por préstamos entre empresas" después de aclarar (vía pregunta) que no se refería al panel de crédito a cobrar sino a cuánto debe cada empresa a las demás. Solo entran las cajas con `ctxLoanNet(ctxId,cur)<0` (deben más de lo que prestaron — `ctxLoanNet>0`, les deben, no es una deuda y no aparece acá). A diferencia de "Crédito vs. efectivo" (que exige un arqueo `fis` cargado), acá el "efectivo" es el `sf` (saldo según movimientos) del último día real de esa caja — la deuda de préstamos no depende de haber hecho un arqueo físico, exigirlo dejaría afuera cajas que nunca cargaron uno. `pctDeuda = deuda/(efectivo+deuda)*100`: por ejemplo, una caja que acaba de recibir un préstamo y todavía no gastó nada da 50/50 (el efectivo recibido por el préstamo iguala exactamente la deuda) — es matemáticamente correcto, no un bug. Todo lo que había antes en Análisis (KPIs de Ingresos/Egresos totales, Evolución del saldo, Composición, Diferencias de caja, Flujo mensual, Principales conceptos, Actividad por día de semana, Resumen mensual detallado) sigue existiendo tal cual, pero ahora vive **colapsado al final** dentro de un `.month`/`.mhead`/`.mbody` (reusa el mismo patrón/CSS y la función `toggleM(this)` que ya usaban los meses del índice, con `data-key="ana-old"` para que `openMonths` recuerde si quedó abierto) bajo el título "Gráficos históricos".
- **"Presupuesto por concepto"** (`renderBudgetPanel`, `#budgetPanel`, panel siempre visible en Análisis, entre "Deuda por préstamos entre empresas" y "Gráficos históricos"): pedido explícito del usuario ("categorías de gastos con presupuesto mensual y alerta"). Reusa el campo **"Concepto"** que ya existe en cada movimiento como clave de agrupación (el mismo texto libre que agrupa "Principales conceptos") en vez de agregar una columna nueva de "categoría" al editor/quick-add — esas grillas ya están muy ajustadas para mobile (ver bullet de Responsive) y sumar un campo ahí hubiese sido mucho más invasivo para el mismo resultado práctico. Un presupuesto = `{id,concept,limit}` guardado en `caja_budgets_v1::<ctxId>::<CUR>` (mismo scoping por caja+moneda que `userDays`, así que cambiar de empresa/proyecto o de moneda muestra los presupuestos de esa caja puntual). Alta/edición/baja con `addBudget()`/`editBudget(id)`/`deleteBudget(id)`, todas con `prompt()`/`confirm()` nativos — mismo patrón ya establecido para CRUD que se usa poco (ver el bullet de protección de créditos: "no hay pantalla de configuración dedicada, a propósito"). `renderBudgetPanel()` calcula el gasto del **mes calendario actual** (`new Date()`, no el último día cargado) sumando `Math.abs(mv.imp)` de los egresos de `real` (la caja activa) cuyo `cpt.trim().toLowerCase()` matchea el concepto del presupuesto, y lo compara contra `limit` con una barra (mismo estilo `.bar-track`/`.bar-fill` que el resto de Análisis, roja si se pasó) + un aviso arriba del panel si hay 1+ presupuestos superados. **Fila propia `.bud-row` en vez de reusar `.bar-row`**: `.bar-row` es una grid de 3 columnas fijas que no tiene lugar para los íconos ✎/🗑 de editar/eliminar (un 4º ítem en una grid de 3 columnas explícitas cae mal, a una fila implícita nueva) — se armó con flexbox+wrap en cambio. Ojo con un detalle de flexbox que salió mal en el primer intento: poner `flex-wrap:wrap` en el contenedor NO alcanza para que un nombre largo pase a su propia línea — con `flex-shrink` normal el navegador prefiere encoger el nombre hasta "…" antes que mandar ítems a una línea nueva (el wrap del contenedor solo entra en juego cuando ya no queda margen para achicar más). Bug real, visto con un concepto largo a 390px. Fix en `@media(max-width:760px)`: `.bud-row .nm{flex:1 1 100%}` — el truco de darle `flex-basis:100%` al nombre lo obliga a ocupar toda la fila él solo y empuja el resto (barra/valor/íconos) a la línea de abajo.
- **"Proyección de flujo de caja"** (`renderCashProjection`, `#cashProjection`, panel siguiente en Análisis, antes de "Gráficos históricos"): pedido explícito del usuario. Toma los últimos 30 días **operados** (`real.slice(-30)`, no 30 días de calendario — puede haber huecos si no se cargó un día) y calcula el cambio neto de saldo entre el primero y el último (`l.sf - f.si`) dividido por los días de **calendario** reales que separan sus fechas (`new Date(l.iso)-new Date(f.iso)`, no la cantidad de entradas) — así el promedio diario no se infla si hay huecos sin cargar. Si el promedio es negativo y el saldo actual (`last.sf`) es positivo, proyecta `Math.floor(saldo/-promedio)` días hasta llegar a $0 y la fecha estimada; si el saldo ya está en $0 o negativo, avisa directo sin proyectar; si el promedio es ≥0, muestra un mensaje neutro/positivo ("sin quiebre proyectado"). Aclarado explícitamente en el texto del panel que es una estimación simple (no contempla estacionalidad ni gastos puntuales grandes que se repitan o no) — no pretende ser una herramienta financiera seria, es una señal de alerta temprana.
- **Selección con suma neta** (`.selbar`, botón `.btn.on`): botón **"☑ Seleccionar"** en el Resumen (`daySelBtn`/`toggleDaySelMode`, junto a "＋ Agregar movimiento") y en "Buscar movimientos" (`movSelBtn`/`toggleMovSelMode`). Agrega una columna de checkboxes (`.selchk`) a la tabla; tildar movimientos muestra una barra celeste con "N seleccionado(s) · Suma neta: $X" (con signo, ingresos suman/egresos restan) y un link "Limpiar selección". En Buscar movimientos la clave de cada ítem es `iso#índice` (`_movByKey`), y al filtrar la búsqueda la selección se recorta sola a lo que sigue visible. Disponible también en días de solo lectura (histórico), ya que sumar no modifica nada — a diferencia de Agregar/Editar, que siguen restringidos a `src==='user'`.
- **Arqueo del día**: "Caja real (conteo físico)" se carga **por partes** (igual patrón que créditos): el editor tiene una lista (`edState.fisMovs`, `#fisMvList`/`renderFisMovRows`/`blankFisMov`) con botón **"＋ Agregar parte del conteo"** — cada fila tiene su propio **toggle ＋/−** (pedido explícito del usuario: "que me permita sumar como el excel") además del importe, así se puede restar un faltante/diferencia dentro de la misma lista en vez de tener que cargar un número ya neteado a mano. "Total contado" se previsualiza en vivo con signo (`recalcFis`, `pesoS`, suma todas las filas aplicando +/−). Al guardar, `saveDay` arma `cajaParts` aplicando el signo de cada fila, `d.caja` = ese array (con negativos donde corresponda) y `d.fis` = su suma (o `null` si no se cargó ninguna parte). El detalle del día (`openDay`) muestra el desglose **"Desglose caja real (conteo físico)"** con un chip por parte (las negativas en rojo, `pesoS`) + Σ total. Compat: un día viejo con `fis`/`caja` como valores sueltos sin signo (de antes de este cambio) se reconstruye como fila(s) **＋** al reabrir para editar — `type` se infiere del signo del valor guardado (`v<0?'out':'in'`), nunca se pierde nada.
- **"Cheques / valores"** sigue siendo un campo único (no itemizado).
- **"Créditos a cobrar"** — es un **saldo acumulado día a día** (como la caja), no un número suelto. El editor muestra "Saldo anterior" (`d.credSi`, heredado del día previo) y una **lista de ajustes** (`edState.credMovs`, `#credMvList`/`renderCredMovRows`/`blankCredMov`) con botón **"＋ Agregar ajuste de crédito"**: cada fila tiene su propio toggle +/-, un campo de **nombre/concepto** ("de quién es", `cm.note`) e importe (igual que los movimientos de caja), así se pueden cargar **varios ajustes el mismo día** sin que uno reemplace al otro. "Nuevo saldo de créditos" se previsualiza en vivo (`recalcCredit`, suma todas las filas). Al guardar, `saveDay` arma `d.credMovs` (itemizado, con `note`) y `d.credAdj` = suma de esas filas. `cascadeChain()` (además de `si`/`sf` de la caja) encadena `credSi`/`cred` a través de todos los días reales usando `credAdj` — corre en cada `saveDay`/`deleteDay`/`deleteMov`/préstamo, y también al inicio de `recompute()` (o sea en cada render) para que quede siempre consistente sin depender de haber guardado algo. Compat: si un día quedó con el `cred` plano de una versión anterior (sin `credAdj`), se trata como el ajuste de ese día la primera vez que se recalcula la cadena — no se pierde nada.
  **Detalle de créditos protegido por clave** (pedido explícito del usuario: "que el detalle de crédito quede oculto, solo pueda verse... con clave"): en el modal de detalle del día (`openDay`), la fila "Créditos a cobrar" del Control de caja muestra el saldo (número) siempre, y al lado tiene un botón **"🔒 Ver"** **siempre visible** (`openCreditDetail()`, sin condicionarlo a `d.cred` — un día sin crédito cargado también lo muestra, para poder configurar la clave o ver el desglose acumulado de otros días aunque el día puntual que se está mirando esté en cero; bug real ya corregido, el botón no aparecía si `d.cred` era 0/null) que abre el desglose por nombre/concepto **solo después de pedir una clave** (`CREDIT_PIN_KEY='caja_credit_pin_v1'`, guardada en este navegador — mismo nivel de seguridad liviano que el código de sync, no es una cuenta real). Sin clave configurada, el primer toque pide crearla (mínimo 4 caracteres); clave incorrecta solo muestra un toast, nunca abre el modal. **Pide la clave cada vez** — hubo un ida y vuelta con el usuario acá: primero pidió explícitamente que quedara desbloqueado para siempre en el navegador (no se le preguntara la clave de nuevo, ni en el modal ni en el editor — se implementó con una `CREDIT_UNLOCKED_KEY` en `localStorage`), y después volvió atrás ("quiero que cada vez que quiera ver los créditos me pida clave") — así que ese mecanismo de desbloqueo persistente se **sacó completamente** (no queda ni la clave `CREDIT_UNLOCKED_KEY` ni las funciones `creditUnlocked()`/`setCreditUnlocked()`): `askCreditPin()` vuelve a pedir el `prompt()` de la clave en cada llamada, sin atajos. `creditNotesBreakdown(ctxId,cur)` suma **todos** los `credMovs` de **todos los días reales** de esa caja (reusa `ctxRealDays`), agrupados por `note`, y `renderCreditDetailModal()` los muestra ordenados de mayor a menor + un total, con un link "Cambiar clave" (solo pide la clave nueva, ya que para llegar ahí adentro ya te autenticaste). **El modal "Ver detalle" es editable** (pedido explícito del usuario: "aun no puedo editar lo oculto", refiriéndose a este modal — hasta entonces era de solo lectura): debajo del total de cada nombre/concepto, `creditNoteEntries(note)` lista cada ajuste individual que lo compone (fecha + importe con signo) recorriendo `userDays` (la caja activa en memoria, **no** `ctxRealDays`/histórico — el histórico importado, `BASE_SEED`, no tiene `credMovs` en absoluto porque es de antes de que existiera esta función, así que nunca hay una fila de solo-lectura que mezclar acá y el total de `creditNotesBreakdown` siempre cuadra con la suma de estas filas). Cada fila tiene ✎/🗑 (`editCreditNoteEntry(iso,idx)`/`deleteCreditNoteEntry(iso,idx)`, mismo patrón `.rowact`/`.rowact.del` que días/movimientos/préstamos): editar pide con `prompt()` el nombre nuevo y después el importe nuevo **con signo** (negativo = baja del crédito, igual convención que `cm.imp` ya guardado — se le avisa en el texto del prompt); ambas funciones recalculan `credAdj` de ese día sumando su `credMovs` y llaman `cascadeChain(activeContextId,CUR,userDays)` + `saveUser()` (mismo flujo que `deleteMov`) antes de refrescar `renderAll()` y volver a pintar el propio modal (`renderCreditDetailModal()`) para que el total se actualice en el momento. Solo se pueden editar ajustes de días `src:'user'` (por construcción, ya que `creditNoteEntries` solo mira `userDays`) — no hace falta chequeo extra para histórico. El viejo panel **"Detalle de créditos del día"** que antes vivía sin protección en `openDay()` (mostraba los `note` de ESE día nada más) se **eliminó** — quedaría inconsistente tener un desglose sin proteger al lado de uno protegido con la misma información. `openCreditDetail()`/`changeCreditPin()` usan `prompt()` nativo — no hay pantalla de configuración dedicada, a propósito, para no sumar UI a algo que se usa poco. **La misma protección se extiende al editor de día** (bug real: el usuario reportó que veía el detalle sin clave — resultó ser que la protección de arriba solo cubría el modal de solo lectura; el propio editor de "＋ Nuevo día"/`editDay` mostraba los nombres/conceptos de créditos ya guardados en inputs de texto planos y editables, sin ninguna protección, algo que se detectó recién con una captura de pantalla del usuario). En `openEditor()`, cada fila de `edState.credMovs` cargada desde un día existente (`existing.credMovs`/`existing.credAdj`) se marca con una bandera en memoria `_preexisting:true` (nunca se persiste: `saveDay()` arma un objeto `{imp,note}` nuevo, así que la bandera no llega a `localStorage`). Una variable `credRowsUnlocked` (declarada junto a `edState`, se resetea a `false` en cada `openEditor()` — hay que volver a poner la clave cada vez que se abre el editor, aunque sea la misma sesión de la página) controla si esas filas se muestran. `renderCredMovRows()` separa las filas de `edState.credMovs` en tapadas (`cm._preexisting && !credRowsUnlocked`) y visibles, y en vez de una línea "Oculto" **por fila**, junta **todas** las tapadas en una única línea resumen (`.mvrow-locked`, arriba de las filas visibles): `🔒 N ajuste(s) de crédito oculto(s)` + botón **"🔒 Ver"** — pedido explícito del usuario ("que esté todo oculto en una línea sola y luego se despliegue"). Ni el nombre ni el monto de ninguna fila tapada se ven hasta desbloquear (pedido previo: "que quede oculto todo el concepto y tb el monto"); el **total** ("Nuevo saldo de créditos") sigue siempre visible y calculado bien porque `recalcCredit()` suma `cm.amt` directo del array en memoria, sin depender de que las filas estén pintadas. Las filas nuevas que el usuario tipea en esa sesión (sin `_preexisting`) siempre están entre las "visibles", nunca se agrupan en el resumen tapado. `unlockCreditRows()` pide la clave (compartiendo el helper `askCreditPin(enterMsg)` con `openCreditDetail()`) y, si es correcta, pone `credRowsUnlocked=true` y vuelve a renderizar: la línea resumen desaparece y las filas que agrupaba se despliegan como filas editables normales, con nombre e importe reales. El header "Créditos a cobrar" del editor tiene además un botón **"🔒 Ver detalle"** que llama a `openCreditDetail()` (el mismo desglose acumulado de TODA la caja que usa el modal de detalle del día) — bug real corregido: en un día **nuevo** (sin `credMovs` propios todavía, `edState.credMovs:[]`) no aparecía ninguna línea tapada porque no hay nada que enmascarar para ESE día puntual, así que no había forma de ver de quién es el "Saldo anterior" heredado sin salir del editor; este botón cubre ese caso siempre, sin depender de si el día que se está editando tiene ajustes propios.
  **Huella digital / Face ID como alternativa a la clave** (pedido explícito del usuario, parte de la tanda "hacelo más pro"): `askCreditPin()` (compartida por `openCreditDetail()` y `unlockCreditRows()`) ahora, si hay una clave ya configurada y `creditBiometricEnabled()` (`caja_credit_biometric_v1`), intenta primero `tryBiometricUnlock()` (`Capacitor.Plugins.NativeBiometric.verifyIdentity(...)`, plugin `@capgo/capacitor-native-biometric` — ver bullet de notificación diaria para por qué este patrón de plugin sin bundler) **antes** del `prompt()`; si falla, cancela o no está disponible, cae directo al `prompt()` de siempre, nunca deja sin forma de entrar. **Importante, no confundir con el desbloqueo persistente que se sacó (párrafo de arriba)**: esto NO es "una vez desbloqueado, nunca más pide nada" — sigue pidiendo verificación **cada vez** que se abre el detalle, solo cambia el método (huella en vez de tipear) — respeta la decisión explícita del usuario de que se pida siempre. Nunca reemplaza la clave: la clave sigue siendo obligatoria para configurar/cambiar (`changeCreditPin()`), la huella es solo un atajo posterior. Se ofrece **una sola vez** (`offerCreditBiometric()`, un `confirm()` nativo) justo después de configurar la clave por primera vez o de cambiarla — si el usuario dice que no, sigue usando la clave sin que se le vuelva a preguntar. En la versión web/PWA (`isNativeApp()` falso, `window.Capacitor` no existe) esto queda completamente inerte y el flujo es exactamente el de antes.
- **Diferencia de caja = movimientos vs (caja física + créditos)**: `dif` compara "Saldo según movimientos" (`sf`) contra el **"Saldo final (caja + créditos)"** = `fis + cred` — los créditos a cobrar cuentan como efectivo para esta comparación, a pedido explícito del usuario ("para la diferencia de caja los créditos se suman al efectivo y ese es el saldo final"). `chq` (cheques) sigue siendo solo informativo, no entra en el cálculo. `cascadeChain()` es la única fuente de verdad de `dif` (lo recalcula siempre, junto con `credSi`/`cred`, para todos los días reales en cada pasada — los sitios que guardan un día calculan un `dif` provisorio con la fórmula vieja, pero `cascadeChain()` lo corrige inmediatamente después). El modal de detalle (`openDay`) muestra la línea **"Saldo final (caja + créditos)"** justo antes de "Diferencia de caja" para que se entienda de dónde sale la comparación. (Nota: este dato es independiente del desglose Efectivo/Crédito de los KPIs del Resumen, que reparte el `sf`/`si`/`ing`/`egr` de movimientos puros — ver bullet de Resumen.)
- **🖨 Vale de Caja** (`printVale(i)`, botón impresora en cada fila editable de "Créditos a cobrar" del editor): pedido explícito del usuario, con una foto de un talonario físico de "Vale de Caja" como referencia de formato — comprobante para dejar constancia en papel de que se otorgó un crédito/adelanto. Arma un documento imprimible (franja bordó `#7a1330`, título "Vale de Caja", caja con el nombre de la empresa/proyecto) con **"Importe (en letra)"** (`importeEnLetras(amount,cur)`, ej. "Cien mil pesos con 50/100") + el monto en números, **"Concepto"** (el nombre/nota de esa fila) y un pie con **Fecha** (la del día que se está cargando/editando) / **Autorizado por** / **Recibido por** en blanco, para firmar a mano después de imprimir — no hay forma de completar esos dos últimos digitalmente, a propósito, es una firma física. `numeroALetras(n)` convierte un entero a su forma escrita en español (hasta cientos de millones, con las excepciones de "cien"/"veintiuno"/etc.); `importeEnLetras` lo envuelve agregando la moneda ("peso"/"pesos" según singular/plural) y aplica la apócope "veintiún"/"un" antes del sustantivo **solo ahí** (no dentro de `numeroALetras`, que debe devolver "veintiuno"/"uno" tal cual para otros usos, ej. dentro de "mil uno"). Valida que la fila tenga nombre e importe cargados antes de imprimir (si no, `toast()` y no abre nada). Reusa el mismo `#overlay`/`#modal` que el resto de los modales de solo lectura, pero como se abre **mientras el editor (`#edOverlay`) sigue abierto atrás** (a propósito: cerrar el editor podría perder un día nuevo sin guardar todavía), hubo que resolver 2 problemas de superposición, ambos porque comparten `z-index:100` y **ambos** quedan con la clase `.on` a la vez: (1) en pantalla, el editor (que va después en el DOM) tapaba el vale — se le subió el z-index a `#overlay` (`110`) para que gane siempre; (2) al imprimir, el CSS `@media print` mostraba **los dos** superpuestos (todo el formulario del editor + el vale) porque la regla `.overlay:not(.on)` no oculta a ninguno de los dos si ambos están abiertos — se agregó `#overlay.on ~ #edOverlay{display:none!important}` para tapar el editor en la hoja impresa específicamente cuando el vale está encima. La construcción del HTML del vale se separó en `printValeFor(amt,note,type,fechaIso)` (sin depender de `edState`/DOM) — `printVale(i)` es un wrapper fino que lee la fila `i` y le delega. **Auto-imprimir al otorgar un crédito** (pedido explícito del usuario: "que me imprima un vale de caja cuando otorgo un crédito", sin tener que tocar el 🖨 manualmente): `saveDay()` calcula `newGrants` = las filas de `edState.credMovs` que son **nuevas en esta sesión** (`!cm._preexisting`, se acaban de tipear, no ya guardadas de antes) **y** de tipo `'in'` (otorgar, no una devolución) con nombre e importe válidos, ANTES de cerrar el editor. Si hay al menos una, al terminar de guardar el día **no** abre el resumen del día como siempre — abre directo `printValeFor()` del primer crédito otorgado y dispara `window.print()` automáticamente (con los mismos `setTimeout` cortos que ya usaba `printDay()` para dejar pintar el DOM antes de imprimir). Si se cargó más de un crédito nuevo en el mismo guardado (caso raro), solo se autoimprime el primero — el resto se puede imprimir después reabriendo el día y usando el 🖨 de cada fila (una vez desbloqueadas con la clave, ya que al guardar pasan a ser `_preexisting`). Reeditar un día sin agregar créditos nuevos (todos `_preexisting`) sigue abriendo el resumen del día normal, sin autoimprimir nada.
- **⬇ Excel** (`downloadExcel`): genera un `.xlsx` real multi-hoja **sin librerías** (ZIP+OOXML a mano: `zipStore`/`buildXlsx`). Hojas: Resumen diario, Movimientos, Préstamos — todas las empresas + histórico.
- **⤓ Respaldo** (`openBackupPicker`/`backupBtn` + `importFile`): exporta/importa el respaldo `.json` (re-importable). **Bug real corregido** (pedido explícito del usuario: "que haga un backup de todas las empresas"): el botón exportaba solo `userDays` (la caja activa, un array plano), no `fullBackupPayload()` — un "respaldo" manual que en los hechos no respaldaba nada del resto de las empresas/proyectos ni los préstamos, aunque el importador de acá al lado (`importFile.onchange`) ya soportaba el formato completo desde antes (se usa para el backup por email y para la nube). **Segundo pedido, sobre el fix anterior** ("que deje elegido si todas o cuáles"): en vez de exportar siempre todo sin preguntar, `backupBtn.onclick` abre un modal picker (`openBackupPicker`, mismo patrón `#modal`/`#overlay` que `openSyncSettings`) con un checkbox **"Todas las empresas/proyectos"** + una fila por cada una (con la cantidad de días cargados al lado, tildadas por default) — el checkbox "Todas" sincroniza a todas las filas y viceversa (se destilda solo si alguna fila individual queda sin tildar). El payload armado a partir de lo tildado ya no usa `fullBackupPayload()` tal cual (esa función siempre trae TODO): arma el mismo `{exportedAt,contexts,data,loans}` pero filtrando `contexts`/`data` a los ids seleccionados, y los **préstamos** entran si **al menos una punta** (`from.id` o `to.id`) está tildada — el objeto de préstamo ya guarda su propia empresa/proyecto embebida, así que sigue siendo legible en el detalle aunque la contraparte haya quedado afuera del archivo. El nombre del archivo distingue `respaldo_completo_...` (si se tildó todo) de `respaldo_<empresa(s)>_...` (selección parcial). El botón **"Importar un respaldo"** vive ahora adentro de este mismo modal (antes era el branch `else` del `confirm()` viejo) y sigue abriendo el `#importFile` de siempre, sin cambios en la lógica de import. Verificado con Playwright: selección parcial exporta solo esas empresas y filtra bien los préstamos, selección completa reproduce el backup de siempre, "Todas" tildar/destildar en cascada, aviso si no se tildó nada o si lo tildado no tiene días cargados, y un roundtrip completo (exportar → borrar todo → reimportar) vuelve exactamente igual.
- **✉ Backup diario** (`openBackupSettings`): panel/modal con guía in-app. Métodos:
  - **Google Apps Script** (recomendado, sin límite de tamaño): POST `no-cors` a la URL `/exec`; el script del usuario manda el mail con el `.json` adjunto. La guía + el código están dentro del modal, generados por `appsScriptCode(email)` con los datos reales ya cargados (config de Firebase, código de sync, email) — copiar y pegar, sin que el usuario tenga que completar nada a mano. Ojo con el deploy: "Quién tiene acceso" tiene que ser **Cualquiera** — con "Solo yo" el POST se rechaza y, como es `no-cors`, la app no se entera y muestra "enviado" igual (bug real ya diagnosticado con un usuario).
    - **Por defecto** (sin configurar nada más) el envío depende de que alguien **abra la app** ese día (`maybeAutoBackup`, 1×/día, guarda la fecha en `caja_backup_last_v1` — es local a ese navegador, no viaja con la sincronización en la nube).
    - **Automático de verdad, sin depender de abrir la app** (`dailyBackup`, incluida en el mismo script generado): usa `UrlFetchApp` para autenticarse anónimo contra la Identity Toolkit REST API de Firebase y leer directo el documento de Firestore (`cajas/{codigo}`) por HTTP, sin pasar por el navegador. Requiere que el usuario agregue un **disparador de tiempo** en Apps Script (Activadores → función `dailyBackup` → diario) — la guía in-app lo explica paso a paso. Corre en el servidor de Google a la hora que elijas, todos los días, hayas abierto la app o no.
    - Para que `dailyBackup` pueda leer la nube por HTTP sin interpretar el formato tipado de Firestore, `cloudPayload()` (la sync) agrega un campo extra **`backupJson`** = `JSON.stringify(fullBackupPayload())` — una copia plana en string del mismo payload que ya se sincroniza como campos nativos. `startSync()` hace un **backfill** automático (`schedulePush()`) la primera vez que conecta a un documento que todavía no tiene `backupJson`, así los usuarios que ya tenían sync activado antes de este cambio no necesitan tocar nada para que aparezca.
    - **Troubleshooting "no me llegó el backup"** (caso real ya diagnosticado): lo primero es pedirle al usuario que abra **script.google.com → su proyecto → ⏰ Ejecuciones** y vea si hay alguna corrida de `dailyBackup` ese día. (a) Si no aparece ninguna ejecución de `dailyBackup`: el disparador de tiempo nunca se configuró, o el proyecto tiene una versión vieja del código que solo tiene `doPost` (de antes de que existiera `dailyBackup`) — hay que decirle que vuelva a copiar el código desde el modal (se regenera con `appsScriptCode()`, siempre incluye ambas funciones) y lo pegue entero, sin necesidad de volver a "Implementar" (el disparador corre siempre contra el código guardado más reciente del proyecto, no contra la versión publicada como app web). (b) Si ejecuta `dailyBackup` a mano y tira error: probablemente `backupJson` no está en el documento — se puede verificar sin acceso a la cuenta del usuario, autenticando anónimo por REST contra Identity Toolkit con el `apiKey` del `firebaseConfig` y leyendo el doc de Firestore por HTTP (`GET .../databases/(default)/documents/cajas/<codigo>`), igual que hace `dailyBackup` — si `fields.backupJson` no está, la sync nunca se conectó del lado de ese usuario. (c) Ejecutar `doPost` a mano desde el editor de Apps Script SIEMPRE tira `TypeError: Cannot read properties of undefined (reading 'postData')` — es esperable (esa función espera una petición HTTP real, no una ejecución manual) y no es el bug; para probar a mano hay que elegir `dailyBackup` en el desplegable de funciones, no `doPost`.
  - **EmailJS** (alternativo; el plan free limita el tamaño → puede fallar con respaldos grandes). No tiene versión "server-side" — depende de que se abra la app.
  - **Borrador manual** (mailto + descarga). Es el fallback si el envío automático falla.
- **☁ Cuenta y sincronización (Mess)** — *reescrito y verificado; reemplaza la descripción vieja de "código de caja" + Firestore + auth anónima, que ya no existe.*
  - **Una sola cuenta para las dos apps.** Se entra con mail/clave o Google contra el MISMO Firebase que el sistema de gestión (`modo-prueba-bb8c2`). Como los dos sitios están en `speranzaemiliano-rk.github.io`, la sesión es la misma: si entraste al sistema, la caja te reconoce sola (`startSync` → `onAuthStateChanged` → `conectarCaja`).
  - **Dónde viven los datos:** Realtime Database, `cajaDiaria/<uid>`. **Requiere la regla correspondiente en `database.rules.json` del repo `mi-app`** — sin ella la raíz está en `".read": false` y Firebase niega todo en silencio. Fue una caída real: la caja nunca subía nada y cualquier navegador nuevo abría vacío.
  - **Fusión, nunca reemplazo.** Antes se comparaba la CANTIDAD total de días y ganaba entero el lado con más: un dispositivo con un día de más subía su copia completa y borraba de la nube lo cargado desde los otros. Ahora `fusionarCajas(local, remoto)` resuelve **día por día** con `_elegirDia`: si ambos lados tienen sello `mod` gana el más nuevo; si alguno no lo tiene (días anteriores a la v71) gana el que tiene **más movimientos**; un día presente en un solo lado se conserva siempre. Contextos y préstamos se unen por id. **Nunca se borra un día.**
  - **El sello `mod`** lo pone `_sellarCambios()` dentro de `saveUser`/`saveUserDaysFor` —el único punto por donde pasan todos los guardados— comparando contra lo ya almacenado: sólo se sella el día que realmente cambió, y si no cambió nada se conserva el sello anterior.
  - **Multiusuario / multidispositivo: se sube con `transaction()`, nunca con `set()`.** `set()` reemplaza el nodo entero con lo que este dispositivo tenía en memoria; si dos pestañas o dos dispositivos del mismo usuario guardan casi a la vez, el segundo pisa al primero. Fusionar antes no alcanza: entre leer y escribir el otro ya pudo haber escrito. `_subirFusionando()` usa `_ref.transaction(remoto => fusionarCajas(local, remoto||{}))`, que Firebase reintenta si el nodo cambió debajo. **No volver a usar `_ref.set()` para subir la caja.**
  - **Importar un respaldo también fusiona** (`importFile.onchange` usa `_elegirDia`). Antes ganaba siempre el archivo, así que importar un respaldo viejo borraba lo cargado después en ese dispositivo.
  - **Barra de estado (`#cloudBar`, `_pintarEstadoNube`)**: gris = sincronizada, con qué cuenta y hace cuánto; roja = sin sesión (con acceso directo a entrar) **o** sincronización fallida, con el motivo. `permission_denied` se traduce a "falta la regla de la rama cajaDiaria". **Ojo al tocar esto:** `conectarCaja` setea `_user` ANTES de leer la base, así que "hay usuario" no implica "sincroniza" — esa confusión hizo que la barra dijera "Sincronizada" mientras Firebase rechazaba todo.
  - **Trampa ya pisada dos veces:** el chequeo de sintaxis (`new Function`) NO detecta un identificador inexistente. Al pasar a la fusión se borró `_diasEn()` y quedó llamada en la rama "nube vacía" de `conectarCaja` — o sea que reventaba justo en la PRIMERA sincronización. Las pruebas ejercitaban `fusionarCajas()` de forma directa sin pasar nunca por `conectarCaja()`. **Probar el camino real con un `_ref` simulado, no sólo las piezas sueltas.**
  - Claves de localStorage vigentes: `caja_device_id_v1`, `caja_user_days_v2::<ctxId>::<CUR>`, `caja_contexts_v1`, `caja_loans_v1`. (Las viejas `caja_sync_cfg_v1`/`caja_sync_code_v1`/`caja_sync_enabled_v1`/`caja_sync_autoinit_v1` ya no se usan.)
  - **Candado de 4 dígitos** para abrir la caja: `PIN_KEY`, hash guardado, ver `abrirCandado()`/`_pinIntento()`.
  - Ver `MIGRACION.md` para el formato del respaldo `.json` campo por campo.
- **🔔 Notificación diaria de arqueo** (`openNotifSettings`, botón "Notificación diaria" en el menú "⋯" junto a Backup diario/Excel/Nube/Respaldo; pedido explícito del usuario, parte de la tanda "hacelo más pro"): solo funciona en la **app nativa instalada** (Android/iOS vía Capacitor, ver `MOBILE.md`) — en el navegador/PWA el modal muestra un mensaje explicando eso y no hay nada más para hacer ahí. Usa `@capacitor/local-notifications`, accedido directo como `Capacitor.Plugins.LocalNotifications` **sin import ni bundler**: Capacitor inyecta el runtime nativo (`window.Capacitor`) solo cuando la app corre dentro del wrapper nativo, y cualquier plugin nativo sincronizado con `cap sync` queda expuesto ahí automáticamente por nombre — el paquete de npm solo hace falta como dependencia para que `cap sync` lo empaquete en `android/`/`ios/`, `app.html` en sí sigue sin build. `isNativeApp()` (`window.Capacitor?.isNativePlatform?.()`) es el gate que usan esta función y la de huella digital (ver bullet de protección de créditos) para no romper nada en la versión web. Config: `caja_notif_enabled_v1` (on/off) + `caja_notif_hour_v1` (0-23), elegidos en el modal; activar pide permiso (`ensureNotifPermission()`) en el momento, no antes. **No es un recordatorio recurrente fijo del sistema operativo** — `scheduleArqueoNotif()` se llama al final de `renderAll()` (o sea después de cualquier carga/edición de día, cambio de caja, etc.) y reprograma un **único** aviso (id fijo `90001`, siempre se cancela el anterior antes de reprogramar para no duplicar) para la hora elegida de hoy (o mañana si esa hora ya pasó) — pero **solo si la caja activa todavía no tiene el día de hoy cargado** (`userDays.some(d=>d.iso===todayIso())`); si ya está cargado, cancela y no reprograma nada. Limitación v1 documentada a propósito: solo mira la **caja activa**, no todas las empresas/proyectos como sí hace la alarma de diferencia de caja (`computeGlobalWarnings`) — cambiarlo a multi-caja sumaría complejidad para un caso de uso (recordatorio personal de "cerrá la caja de hoy") donde alcanza con la caja que se usa día a día. Como es un aviso único reprogramado (no un cron nativo), si la app no se vuelve a abrir después de la última carga, el último aviso programado en esa sesión igual llega — mismo principio que ya se documentó para `dailyBackup` vs. depender de abrir la app.
- **Responsive / mobile** (`app.html`): breakpoints en `@media(max-width:1000px)` (sidebar colapsable) y `@media(max-width:760px)` (grillas de formularios). El sidebar (`.side`) es `position:fixed` con `width:280px;max-width:82vw` y un `#sideScrim` (fondo oscuro) que aparece atrás — `toggleSideMenu()`/`closeSideMenu()` manejan ambos a la vez; antes el sidebar no tenía `width` explícito y el offset `left:-260px` no lo escondía del todo (bug real, corregido). Varias secciones arman su grilla con **`style="grid-template-columns:..."` inline** (Préstamo entre empresas, Cambio de divisa, la fila Fecha/Saldo inicial/Día del editor, la fila de ajuste de crédito) — un inline style le gana a cualquier regla de la media query sin `!important`, así que esas quedan apuntadas una por una: `.loanbox .fgrid,#v-loans .fgrid`, `#credMvList .mvrow`, `#edHeadGrid` (todas forzadas a 1 columna en mobile). Las tablas anchas (`#monthTable`, `#movTable`, `#loanTable`, el detalle de movimientos del día `.mvs`, y ahora también cada tabla de mes dentro de "Movimientos diarios") están envueltas en `<div style="overflow-x:auto">` — en vez de rediseñarlas para mobile, se dejan scrollear horizontalmente dentro de su propio contenedor (mismo patrón en toda la app).
  **Auditoría mobile pedida explícitamente ("que quede como una app")**, hecha con capturas reales a 390px (no solo mirando el CSS), encontró y corrigió 3 problemas concretos:
  1. **Menú "⋯" de acciones secundarias** (`#actionsMoreBtn`/`#actionsSecondary`, dentro de `.actions`): en mobile, mostrar los 6 botones de la topbar (Backup diario/Excel/Nube/Respaldo/Instalar/Nuevo día) sueltos y apilados ocupaba media pantalla arriba de cualquier vista, antes de llegar al contenido. Ahora solo "＋ Nuevo día" (la acción principal) queda siempre visible; el resto vive dentro de `#actionsSecondary`, que en desktop es `display:contents` (fluye igual que antes, sin wrapper visual) y en mobile se convierte en un menú desplegable (`position:absolute`, se abre con `#actionsMoreBtn`, se cierra clickeando afuera, eligiendo una acción, o con Escape). **Ojo con el orden en el CSS**: la regla base `.actions-secondary{display:contents}` está textualmente *después* del bloque `@media(max-width:1000px)` en el archivo — sin `!important` en las reglas de la media query, la base la pisaba sin importar el ancho de pantalla (bug real, ya pasó una vez al implementar esto). Mismo patrón que ya usa `.menu-toggle{display:grid!important}` en esa misma media query.
  2. **Cabecera de mes cortada** (`.mhead .mstats` en el acordeón de "Movimientos diarios"): Ingresos/Egresos/Cierre en una fila no entraban en 390px y "Cierre" quedaba literalmente truncado e ilegible, sin scroll posible (a diferencia de las tablas anchas, que si tienen `overflow-x:auto`). En `@media(max-width:760px)` ahora solo se muestra "Cierre" (`.mhead .mstats div:nth-child(1),.mhead .mstats div:nth-child(2){display:none}`) — el resto del detalle sigue disponible al expandir el mes.
  3. **`font-weight:600`/`800` sin cargar**: el `<link>` de Google Fonts solo pedía pesos `400;500;700` (heredado de antes de este rediseño), pero el CSS usa `600` (55 usos, ya existía antes) y ahora también `800` (agregado en el rediseño "Apple Store" para los títulos grandes). Cuando el navegador no tiene el peso exacto disponible en la fuente cargada, cae a una tipografía serif del sistema en vez de sintetizar el bold — se notaba fuerte en los títulos grandes nuevos ("Resumen ejecutivo" se veía en una serif rara). Fix: `family=Roboto:wght@400;500;600;700;800`. **Nota para verificar en este sandbox**: Playwright bloquea la red del navegador headless acá (`ERR_CONNECTION_RESET` en las capturas), así que este fix específico no se pudo confirmar visualmente en este entorno — sí se confirmó por `curl` que `fonts.googleapis.com` responde 200 y devuelve los 5 pesos.
  4. **Botones del panel "Movimientos del día" fuera de pantalla** (encontrado en una auditoría posterior, pedido explícito: "hay botones que quedan fuera de pantalla"): `.ph` (cabecera de panel, 16 usos en toda la app: título a la izquierda + grupo de botones a la derecha en una sola fila) no tenía ningún manejo mobile — cuando coincidían varios botones visibles a la vez (☑ Seleccionar / 🖨 Imprimir / ＋ Agregar movimiento / ✎ Editar día en el panel de Resumen), el grupo de botones no entraba al lado del título en 390px y se salía 31px del ancho de pantalla en vez de acomodarse (confirmado midiendo `document.documentElement.scrollWidth` con Playwright, no solo mirando capturas). Fix en `@media(max-width:760px)`: `.panel .ph{flex-wrap:wrap}` + `.panel .ph>div:last-child{flex-wrap:wrap}` — el título pasa a quedar arriba y los botones se apilan debajo en su propia fila, con wrap interno si hiciera falta con más de 2 botones a la vez.
  5. **Análisis: números y textos largos cortados/invisibles en mobile** (pedido explícito: "en analisis algunas indicaciones queda afuera"; luego confirmado con más detalle: "en credito vs efectivo y deuda prestamos no solo que no tiene los graficos sino que los datos quedaron fuera"), dos causas distintas encontradas midiendo con Playwright (`scrollWidth`/`clientWidth` a nivel de página y por elemento, no solo mirando capturas):
     - **Doble padding angosta el contenido**: `.wrap` (contenedor de toda vista) tenía el mismo `padding:34px 40px 80px` que en desktop — 80px de los 390px de un celular (20%) ya perdidos antes de que arranque cualquier panel. Adentro de "Gráficos históricos" (`#anaOldBody`, la sección colapsada de Análisis) hay además un `<div style="padding:18px 20px 20px">` con estilo inline — sumado a `.wrap`, dejaba solo ~268px reales de ancho para el contenido. Con eso, los números grandes de `#anaKpis` (`.kpi .num{white-space:nowrap}` + `.kpi{overflow:hidden}`, ej. "$ 1.925.484.886") quedaban con dígitos literalmente tapados por el borde de la tarjeta (no truncados con "…": directamente invisibles), y "Diferencias de caja" perdía el nombre del día de la semana. Fix en `@media(max-width:760px)`: `.wrap{padding:20px 16px 56px}` + `#anaOldBody>div{padding:16px 10px 20px!important}` (el `!important` es necesario para ganarle al style inline).
     - **Columna de nombre de ancho fijo en filas tipo "barra"**: "Principales conceptos" (`#topConcepts`, usa el `.bar-row{grid-template-columns:120px 1fr auto}` de base) y "Crédito vs. efectivo"/"Deuda por préstamos entre empresas" (`#creditCashRatio`/`#loanDebtRatio`, cada fila fuerza `style="grid-template-columns:190px 1fr auto"`) reservan una columna de nombre de ancho **fijo** para dejarle lugar a la barra de progreso y al valor — en 390px eso no solo corta el nombre con "…", sino que en los paneles de 190px la fila entera (190 + 96 de `.val` + gaps) no entraba en el ancho del panel y la barra/el valor quedaban directamente empujados fuera del área visible (**"no tiene los gráficos... los datos quedaron fuera"**, confirmado por el usuario mirando el celular). Fix: en mobile estas filas se apilan — el nombre pasa a ocupar toda la fila arriba (puede usar 2 líneas), y la barra + el valor quedan en una segunda fila abajo, vía `grid-template-areas:"nm nm" "bar val"` con `grid-template-columns:1fr auto!important` (scoped a `#topConcepts .bar-row,#creditCashRatio .bar-row,#loanDebtRatio .bar-row` para no tocar otras filas `.bar-row` con 2 columnas que ya andaban bien, como "Diferencias de caja").
     **Ojo si se agrega otro panel con `.bar-row` y una columna de nombre fija** (`120px`/`190px`/`100px` inline): en mobile puede sufrir el mismo problema — sumarlo a la lista de selectores del fix de arriba en vez de duplicar la regla.
  6. **Menú "⋯" desalineado** (`#actionsSecondary`, el desplegable de Backup diario/Excel/Nube/Respaldo del punto 1): usaba `right:0`, que lo alinea con el borde derecho de `.actions` — pero `.actions` contiene **los dos** botones ("⋯" y "＋ Nuevo día") juntos, no solo el "⋯" que lo abre. Con el menú (`min-width:220px`) más ancho que el propio `.actions` en mobile, `right:0` lo tiraba 18px afuera del borde izquierdo de la pantalla, quedando parcialmente invisible/cortado (reportado por el usuario: "cuando apreto los tres puntos... quedan desfazados"). Fix: `left:0` en vez de `right:0` — así el menú arranca en el borde izquierdo de `.actions`, que coincide con el botón "⋯" (primer hijo), en vez de con el borde derecho de "＋ Nuevo día".

## Convenciones / cómo trabajar acá

- Editar el HTML directamente (no hay build). Mantener el estilo existente (voseo, tema oscuro en la app, claro en el home).
- **Verificar con Playwright** antes de dar por hecho un cambio (es una app de dinero):
  - Servir: `python3 -m http.server 8099` en la raíz del repo.
  - Node + Playwright: el módulo está en `/opt/node22/lib/node_modules` → importar con ruta absoluta
    `import pw from '/opt/node22/lib/node_modules/playwright/index.js'`. Chromium ya está instalado (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`).
  - Sembrar estado por `localStorage` con `addInitScript` antes de `goto`.
  - Para validar `.xlsx`: `pip install openpyxl` y abrirlo.

## Flujo de git / deploy

- Rama de trabajo: **`claude/web-page-artifact-n434pw`**. Base: `main`.
- Ciclo: commit → push a la rama → PR a `main` → merge → **Pages redepliega solo**.
- Si el PR anterior ya está mergeado, reiniciar la rama desde `main` (`git reset --hard origin/main`) y seguir con el cambio nuevo.
